<p id="notice"><%= notice %></p>

<h1>Listing Groups</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @groups.each do |group| %>
      <tr>
        <td><%= group.name %></td>
        <td><%= link_to 'Montrer', group, class: 'btn btn-info' %></td>
        <td><%= link_to 'Modifier', edit_group_path(group), class: 'btn btn-warning' %></td>
        <td><%= link_to 'Supprimer', group, method: :delete, data: { confirm: 'Êtes vous sur de vouloir supprimer ce groupe?' }, class: 'btn btn-danger' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Ajouter', new_group_path, class:'btn btn-info' %>
<%= link_to 'Home', root_path, class:'btn btn-danger' %>
