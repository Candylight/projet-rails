<%= simple_form_for(@extension) do |f| %>
  <%= f.error_notification %>

  <div class="form-group col-lg-5">
    <%= f.input :name, label: 'Nom', class: 'form-control' %>
    <%= f.input :release, label: 'Date de sortie', class: 'form-control' %>
    <%= f.input :standard, label: 'Autorisée en standard', class: 'form-control' %>
  </div>

  <div class="form-actions">
    <%= f.submit 'Enregistrer ', class: 'btn btn-primary' %>
  </div>
<% end %>
